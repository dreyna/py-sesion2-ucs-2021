"""
Realizar un programa que calcule el area de triangulo.
(b x h)/2
"""
base = float(input("Ingresar la base: "))
altura = float(input("Ingresar la altura: "))

area = (base * altura)/2

print("El area del triangulo es "+str(area))

